import { messages } from '../../../../../common/constants/messages';

const NumberHunt = () => {
  return (
    <section>
      <h1 className={'pageTitle'}>{messages.view.main.tasks.numberHunt.title}</h1>
    </section>
  );
};

export default NumberHunt;
